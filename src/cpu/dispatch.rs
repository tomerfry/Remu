//! Fused decode & execute: pre-specialized straight-line code per opcode.
//!
//! The straightforward interpreter pays for two data-dependent indirect jumps
//! per instruction: `match` on the addressing mode, then `match` on the
//! operation. Here [`dispatch`] instead matches once on the raw opcode byte,
//! and each of the 256 arms is [`exec`] monomorphized with that opcode as a
//! const generic. Inside an arm the [`OpInfo`](crate::cpu::opcodes::OpInfo) is
//! a compile-time constant, so the inlined `resolve`/`execute` matches (and
//! the page-penalty check) constant-fold away, leaving dedicated straight-line
//! code for exactly that instruction behind a single jump-table dispatch.
//!
//! Everything stays inlined into `step()` — no function-pointer calls — so
//! LLVM keeps CPU state in machine registers across the whole instruction.
//! (A fn-pointer handler table was tried first and benchmarked 17–58% *slower*
//! than the plain interpreter: the opaque indirect call forced register
//! spills at every ABI boundary.)
//!
//! This trades binary size (256 specialized code paths per [`Bus`] type) for
//! runtime — the same specialization a JIT would produce, done ahead of time
//! for the whole fixed 256-entry instruction set, with no warmup and no
//! invalidation worries when 6502 code modifies itself.
//!
//! Correctness is inherited, not duplicated: the arms are generated from the
//! same `OPCODES` table and `resolve`/`execute` code the plain interpreter
//! used, so the Tom Harte suite validates this path directly.

use crate::bus::Bus;
use crate::cpu::Cpu;
use crate::cpu::opcodes::OPCODES;

/// The generic per-opcode body. `OP` is the opcode byte, so `info` — and with
/// it the addressing mode and operation — is a compile-time constant in each
/// of the 256 instantiations. Returns the cycles consumed.
#[inline(always)]
fn exec<B: Bus, const OP: u8>(cpu: &mut Cpu, bus: &mut B) -> u8 {
    let info = const { OPCODES[OP as usize] };

    let operand = cpu.resolve(bus, info.mode);
    let extra = cpu.execute(bus, info, &operand);

    let mut cycles = info.cycles;
    if info.page_penalty && operand.page_crossed {
        cycles += 1;
    }
    cycles + extra
}

/// Run the instruction whose opcode byte was just fetched. One jump-table
/// dispatch straight to the specialized code for that opcode.
#[inline(always)]
pub(crate) fn dispatch<B: Bus>(cpu: &mut Cpu, bus: &mut B, opcode: u8) -> u8 {
    macro_rules! dispatch_match {
        ($($op:literal)*) => {
            match opcode {
                $($op => exec::<B, $op>(cpu, bus),)*
            }
        };
    }

    #[rustfmt::skip]
    let cycles = dispatch_match!(
        
    );
    cycles
}
