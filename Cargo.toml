[package]
name = "remu"
version = "0.1.0"
edition = "2024"

[lib]
name = "remu"
path = "src/lib.rs"

[[bin]]
name = "remu"
path = "src/main.rs"

[dependencies]
bitflags = "2"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
debug = true
